# ── Core DICOM & Numerical Processing ─────────────────────────────────────────
pydicom>=2.4.4
numpy>=1.24.0,<2.0.0
scipy>=1.11.0
scikit-image>=0.22.0

# ── DICOM Pixel Data Decompression Handlers ──────────────────────────────────
pylibjpeg>=2.0.0
pylibjpeg-libjpeg>=2.0.0
pylibjpeg-openjpeg>=2.0.0
pylibjpeg-rle>=2.0.0

# ── Image Processing & Geometry ───────────────────────────────────────────────
opencv-python-headless>=4.9.0.80
Pillow>=10.0.0
shapely>=2.0.0
pyclipper>=1.3.0

# ── OCR Engine (PaddleOCR) ───────────────────────────────────────────────────
# NOTE: paddlepaddle is installed separately in Dockerfile as CPU-only wheel
#       to avoid platform-specific build issues.
paddleocr>=2.8.0

# ── NLP & Machine Learning Entity Recognition ─────────────────────────────────
# NOTE: torch & torchvision are installed separately in Dockerfile as CPU-only.
transformers>=4.40.0
gliner>=0.2.0
presidio-analyzer>=2.2.354
spacy>=3.7.4,<3.8.0

# ── Cryptography & De-Identification ──────────────────────────────────────────
pycryptodome>=3.20.0
